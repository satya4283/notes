Ansible Tags
=============
deployment or release new version
Q. we have list of tasks in playbook, I want to run only specific tasks.?
Ans. we make use of ansible tags for running specific tasks.


announce downtime 6hrs
---------------------
stop catalogue service
remove old code -> I can remove /app directory, create again and download new version code
download new code
restart catalogue


ansible include_role vs import_role: 
====================================

We can include other roles using include_role or import_role

include_role --> it will include tasks in run time, it will validate also in run time, it will not pre process.

Tags and when conditions apply only to the include_role statement itself, not the tasks within the role.

import_role --> ansible validate import_role before playbook execution
Tags and when conditions apply to the imported role and its tasks. syntax checking will happen before execution its called pre-processing.

ansible error handling
======================
Error handling means what should we do when error comes, what should we do when error not comes.

if we are able to handle the error, we can keep ignore_errors true and then execute another task in case of failure

id roboshop
	if created already, we are skipping
	otherwise we are creating
	
imagine, there is no user module available

a task executes id roboshop -> task fails if user not available, usually script exit because of this error

another task create user, if rc != 0

vault
============
you can create a file and encrypt it..

ansible-vault create text.yaml
valut password: admin123
set password: PASSWORD:satya123

ansible-vault view text.yaml
ansible-vault  edit text.yaml

cat text.yaml --> you can see only encrypted password

we will decryption password at command line

- vault is not scalable and difficult to maintain  so we have 
SSM parameter store:
===================
we call aws as- platform as service. it is not only used for crating servers/istances.
AWS will provide all the services which are required for running the project.


Linux --> Server, Ansible --> Platform
HashiCorp Vault --> Install, manage, update, upgrade

Linux/ansible integrate with vault softwares

Q. tell me latest improvement you have done in your project.

We were using ansible vault, but there are challenges to maintain the passwords and vaut files, 
so I recently proposed SSM parameter store, 
I did POC and Removed vault and implemented SSM parameter store successfully...




Q. what are tags, when do we use tags.
Q. what is diffrence between import_roles vs include_roles
Q. what is ansible error handling
Q. Erros  you faced recently 
Q. Recent implements you done in your project.
